# Brain Service
apiVersion: v1
kind: Service
metadata:
  name: brain
spec:
  type: ClusterIP
  ports:
    - port: 30000
      targetPort: 30000
      protocol: TCP
      name: http
  selector:
    app: brain
