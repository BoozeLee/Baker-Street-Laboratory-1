apiVersion: v1
kind: Service
metadata:
  name: bsl-api
  namespace: bakerst
spec:
  type: ClusterIP
  ports:
    - port: 5000
      targetPort: 5000
      protocol: TCP
      name: http
  selector:
    app: bsl-api
