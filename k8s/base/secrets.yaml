apiVersion: v1
kind: Secret
metadata:
  name: bsl-api-secrets
  namespace: bakerst
type: Opaque
stringData:
  # These will be populated by deploy script from .env or prompts
  OPENAI_API_KEY: "${OPENAI_API_KEY}"
  ANTHROPIC_API_KEY: "${ANTHROPIC_API_KEY}"
  BSL_API_KEY: "bsl-local-dev-key"
